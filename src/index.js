import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // pas de .js ou autres! => seulement lorsqu'on a du css

ReactDOM.render(<App />, document.getElementById("root"));
